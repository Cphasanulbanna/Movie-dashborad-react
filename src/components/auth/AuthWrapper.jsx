import React from "react";

const AuthWrapper = () => {
    return (
        <section
            className={`flex justify-center items-center h-[100vh] w-[100%] bgc-gradient`}
        ></section>
    );
};

export default AuthWrapper;
